# Deviations — 01-no-reviewer-reload-on-design-writes

- tests/browser/reload.test.ts (AC-20260916-01-5): the embedded-host `vite.config.ts` this test writes needed an explicit `resolve: { alias: { "@": ... "./src" } }` (mirroring the fixture host's own `vite.config.ts`) in addition to `react()`, `tailwindcss()`, `mockReview()` — without it every fixture screen's `@/shells/...`/`@/components/...` imports 500 and the reviewer never renders a screen at all, so the pin/marker assertions would fail for a setup reason, not the spec's reload bug. AC-20260915-03-4's own embedded-host `vite.config.ts` (the pattern this test mirrors per the build brief) omits the same alias, but that test only asserts on `vite build`'s output files/markers and never actually renders a screen, so the gap was invisible there. Forced, not blocking — applying the fixture host's known-good alias config, never touched by the Decisions.
