- [useSyncExternalStore selector stability](feedback_useSyncExternalStore_selectors.md) — never derive new arrays/objects inline in `useStore(selector)`; causes a silent infinite-render hang.
- [Radix Select infinite-loop gotchas](feedback_radix_select_bugs.md) — never pass `value=""`; always pass `position="popper"` on `SelectContent` in this stack.
- [Frame preamble defect (blocked, not mine)](project_frame_preamble_defect.md) — `src/server/plugin.ts`'s `frameHtml()` needs the `@vitejs/plugin-react` dev preamble or the mock iframe never renders. Fixed by the server worker per D18d.
- [Remaining test findings after D18](project_remaining_test_findings.md) — superseded by the render-loop fix below for AC-13/AC-15/AC-18/AC-19's flakiness; AC-18's own coordinate-math note was independently fixed by the test owner.
- [Unstable hook deps under load = infinite loop that only shows up under CPU contention](feedback_unstable_hook_deps_under_load.md) — real root cause of the gate-wide "Maximum update depth exceeded"; `journey?.edges ?? []` in `ScreenPage.tsx` fed a fresh array into `useJourneyGuide`'s deps every render. Read this before ever adding a hook dependency derived with `?? []`/`?? {}`.
- [AC-15 theme flake = Vite optimize-deps 504, not a src/ui/entry.tsx bug](project_vite_optimize_deps_504_under_load.md) — diagnosed via a/b/c questions: none apply; a core JSX-runtime request 504s under CPU load and aborts the frame's whole script, reproduced identically on a plain (non-theme) frame load too. No code fix applied.
