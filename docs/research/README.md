# Research inputs (2026-09-15)

Written by the claude-plugins planning session before this repo existed. Three files:

- `plugin-requirements.md` — every CLI call, JSON shape and host file the spec plugin
  reads from `@555/mock-review`, with `path:line` citations into
  `~/projects/claude-plugins` (Opus investigation).
- `prototype-inventory.md` — what the spike under
  `~/projects/claude-plugins/.claude/worktrees/agent-ae25a43a1031bd49a/prototypes/`
  already has, what to port as reference, what to discard (Opus investigation).
- `fable-rulings.md` — the architect rulings on the eight open design questions.

The brief that consumes them is `docs/brief-01-the-reviewer-package.md`.
