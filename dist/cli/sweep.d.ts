import type { Sweep } from '../schemas/index.js';
export declare function sweepVerb(cwd: string): Sweep;
/** File Plan: text form. One line per queue item, and the queue empty case is one exact line. */
export declare function formatSweepText(sweep: Sweep): string;
//# sourceMappingURL=sweep.d.ts.map